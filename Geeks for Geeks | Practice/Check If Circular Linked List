class GfG
{
boolean isCircular(Node head)
{
// Your code here
if (head == null)
return true;
 
Node curr = head;
 
while (curr != null && curr.next != null) {
curr = curr.next;
if (curr.next == head) {
return true;
}
}
return false;
}
}
