class Solution {
    int getOddOccurrence(int[] arr, int n) {
        // Initialize oddCount to 0
        int oddCount = 0;
        for(int num : arr) {
            oddCount = oddCount ^ num;
        }
        return oddCount;
    }
}
