class Solution {
    static long leftShops(long i, long L){
        // code here
        long ans=0;
        for(int j=1;j<=L;j++)
        {
            ans=((long)(Math.pow(2,j)))-i;
        }
        return ans;
    }
}
