Mean

class Solution {
    static int mean(int N , int[] A) {
        // code here
        int sum = 0;
        for(int i=0;i<A.length;i++){
            sum+=A[i];
        }
        return sum/N;
    }
};
