vector<int> out={};
        int X=A[0];
        for(int i=1;i<N;i++){
            X^=A[i];
        }
        for(int i=0;i<N;i++){
            int o=X^A[i];
            out.push_back(o);
        }
        return out;
