class Solution{
    int maxTripletSum(int a[], int n)
    {
        // Complete the function
        Arrays.sort(a);
        int sum = 0;
        for(int i = n-1 ; i >= n-3; i--){
            sum += a[i];
        }
        return sum;
    }  
}
